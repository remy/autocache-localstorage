{
  "name": "autocache-localstorage",
  "description": "localStorage adapter for autocache",
  "main": "index.js",
  "scripts": {
    "test": "NODE_ENV=test node test/*.test.js | tap-spec",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post"
  },
  "keywords": [
    "autocache",
    "cache",
    "localstorage"
  ],
  "author": "Remy Sharp",
  "license": "MIT",
  "devDependencies": {
    "autocache": "^0.7.0",
    "tap-bail": "0.0.0",
    "tap-spec": "^2.2.1",
    "tape": "^3.5.0",
    "semantic-release": "^4.0.3"
  },
  "dependencies": {},
  "repository": {
    "type": "git",
    "url": "https://github.com/remy/autocache-localstorage.git"
  }
}
